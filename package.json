{
  "dependencies": {
    "flipdown": "^0.3.2"
  },
  "scripts": {
    "start": "parcel index.html",
    "test": "jest",
    "coverage": "jest --collect-coverage",
    "build": "parcel build index.html --dist-dir docs  --no-source-maps --public-url ./",
    "build2": "parcel build index.html --dist-dir docs --public-url ./"
  },
  "devDependencies": {
    "@babel/core": "^7.19.1",
    "@parcel/babel-preset-env": "^2.7.0",
    "babel-jest": "^29.0.3",
    "jest": "^29.0.3",
    "parcel": "^2.11.0"
  }
}
